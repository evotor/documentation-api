swagger: '2.0'
info:
  $ref: ./evotorRestApi/info.yaml
tags:
  - name: Сущности пользователя Эвотор
    description:
      Запросы информации о магазинах, сотрудниках и смарт-терминалах определённого пользователя платформы Эвотор.
  - name: Товары и документы
    description:
      Запросы информации об определённом магазине (`storeUuid`) пользователя платформы Эвотор.
produces:
    - application/json
consumes:
    - application/json
host: api.evotor.ru
schemes:
    - https
paths:
  $ref: ./evotorRestApi/paths.yaml
parameters:
  $ref: ./evotorRestApi/parameters.yaml
definitions:
  $ref: ./evotorRestApi/definitions.yaml
